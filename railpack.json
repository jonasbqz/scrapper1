{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "bun scripts/clean-database.ts && bun run db:migrate:all && nest build && bun scripts/build-engagement-script.ts && bun dist/src/main.js"
  }
}
